#pragma once

namespace VPanic {

	static constexpr int INIT_IMGUI     { 1 << 0 };
	static constexpr int FULLSCREEN     { 1 << 1 };
	static constexpr int VSYNC          { 1 << 2 };

	// i will add more settings for initialising the engine later  'Engine::init()'
}
